import { HighlighterDirDirective } from './highlighter-dir.directive';

describe('HighlighterDirDirective', () => {
  it('should create an instance', () => {
    const directive = new HighlighterDirDirective();
    expect(directive).toBeTruthy();
  });
});
